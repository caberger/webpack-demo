deployment:
  imagePullPolicy: Always
  # change this when stable. Currently we do not take care of version numbers


backend:
  image: ghcr.io/caberger/backend:latest
    
frontend:
  image: ghcr.io/caberger/frontend:latest

ingress:
  host:  demo.cloud.key.fit
